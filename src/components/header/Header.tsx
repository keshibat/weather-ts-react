import './header.styles.css'

const Header = () => (
  <div className="header-container">
    <h1 className="title-text">
      Weather <span className="text-strong">Forecast</span>
    </h1>
  </div>
)

export default Header
